import { TipoConsultaFormatPipe } from "./tipo-consulta-format.pipe";

describe("TipoConsultaFormatPipe", () => {
  it("create an instance", () => {
    const pipe = new TipoConsultaFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
